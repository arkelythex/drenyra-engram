# Tasks — audit-register-closure

> Phase: tasks · Artifact: tasks · Status: draft
> Inputs: spec + design (required). Strict TDD: not active in this phase (planning); `phases.apply.strict_tdd: true` per openspec/config.yaml — apply must run RED → GREEN per slice. `go test ./...` and `npm test` MUST stay green at every slice boundary.
> Forwarded session settings: delivery_strategy=ask-on-risk, chain_strategy=stacked-to-main (owner-approved), artifact_store=openspec.
> Binding: FR-L.*, FR-J.*, FR-Q.*, FR-Z.*, FR-G.*, AC-*, NFR-*, EC-*, IR-*, and FD-1…FD-4 are frozen and MUST NOT deviate. Apply MUST NOT touch any non-test Go/TypeScript file, schema DDL, API signature, adapter decoder, policy semantics, idempotency implementation, or migration implementation unless a conformance test exposes a real defect — that is an exception requiring re-approval (per design).

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ≈ 3,900–5,400 (additions + deletions; nearly all tests/fixtures/docs, no production code) |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | PR J → PR L → PR Q → PR G → PR Z → PR evidence-pass (6 stacked-to-main PRs) |
| Delivery strategy | ask-on-risk |
| Chain strategy | stacked-to-main |

```text
Decision needed before apply: Yes
Chained PRs recommended: Yes
Chain strategy: stacked-to-main
400-line budget risk: High
```

Per-slice line estimates: J ≈ 550–750 (consolidated policy matrix, structural/behavioral override-negative suites, provenance decision); L ≈ 850–1,100 (operation × surface replay catalog, MCP/CLI replay, interrupted-reservation/reopen lost-response proof); Q ≈ 1,000–1,400 (reflected API catalog, exhaustiveness guard, per-method cross-tenant seeds/outcomes, mutation side-effect proof); G ≈ 700–950 (v1…v13 genuine fixture matrix, interruption/reopen proof, migration provenance); Z ≈ 250–350 (operability evidence doc, three reconciliation edits, doc readback test); evidence-pass ≈ 250–400 (audit register G/J/L/Q/Z row updates + whole-change gate proofs). Total clearly exceeds the 400-line rule in openspec/config.yaml (`pr_size`), so the change MUST be delivered as chained PRs stacked-to-main.

**Chain strategy is `stacked-to-main` (already owner-approved).** Each PR merges to main in order J → L → Q → G → Z → evidence-pass. Each is independently verifiable and never leaves a block at an unsupported PASS. No production-code slice exists; if a conformance test exposes a real defect, apply MUST stop that slice and report for re-approval — never weaken an assertion.

---

## PR J — Authorization policy matrix and override-absence (AC-J-1…AC-J-5)

Objective: one consolidated versioned policy matrix across the four frozen policies, structural proof that no supported request/command surface can express an override, behavioral proof that denials are unbypassable at policy/adapter/store boundaries, and the frozen override-absence decision. Files: `internal/authz/consolidated_policy_matrix_test.go` (new), `internal/server/no_override_surface_test.go` (new), `internal/server/override_negative_test.go` (new), `cmd/drenyra-engram/no_override_surface_test.go` (new), `internal/store/authorization_no_bypass_test.go` (new), `contracts/provenance.md`.

- [x] J.1 RED — Write `internal/authz/consolidated_policy_matrix_test.go` with `TestConsolidatedVersionedPolicyMatrix` (AC-J-1, FR-J.1): subtest asserts the four exact version constants `approval-policy/v0.4.0`, `judgment-policy/v0.4.0`, `reconciliation-policy/v0.5.0`, `evidence-lifecycle-policy/v0.8.0`; per-policy representative rows reuse the real public authorization functions + frozen fixture constructors with at least one allow, role denial, scope denial, assurance denial, and SoD denial each; lifecycle adds first/second approval, operational-accountant/admin denial, dual-category-disabled denial, requester-equals-approver denial, same-principal second approval denial; a separate `check-order` subtest copies the ten named probes from `TestLifecyclePolicyCheckOrderIsFrozen` and asserts the exact existing order. RED until the four policy version-exact anchors (`approval_policy_test.go:113`, `judgment_policy_test.go:54`, `reconciliation_policy_test.go:68`, `evidence_lifecycle_policy_test.go:161`) are referenced and the constants are asserted. <!-- sdd-owner: implementation -->
- [x] J.2 GREEN — Make `TestConsolidatedVersionedPolicyMatrix` green using only existing test fixtures/helpers; the four policy-specific suites must remain green and unchanged (NFR-J.2 version freeze). <!-- sdd-owner: implementation -->
- [x] J.3 RED — Write `internal/server/no_override_surface_test.go` with `TestNoOverrideFieldOnRequestSurfaces` (AC-J-2, FR-J.2): reflect over an explicit `[]reflect.Type` registry of canonical exported `core.*Command`/`core.*Input` values accepted by API/HTTP/MCP mutation boundaries, recursively traverse exported fields (visited-type set; structs/pointers/slices/arrays/map element types), normalize Go name + JSON tag, and reject `override`/`breakglass`/`force`/`bypass` and equivalent spellings; adjacent AST guard parses `internal/server/api.go`, finds exported `*core.XCommand`/`*core.XInput` params on exported API methods, and requires each type to be registered or explicitly classified read-only; doc comment cites the deliberate-absence comments at `evidence_lifecycle_policy.go:38`, `purge_store.go:32/856/1010/1237`, `store.go:383/390`, `auth/errors.go:104`, `purge_execution_store.go:573`. No comment/output-type scanning. RED until every canonical request type is registered and no forbidden spelling is accepted. <!-- sdd-owner: implementation -->
- [x] J.4 RED — Write `cmd/drenyra-engram/no_override_surface_test.go` with `TestCLINoOverrideFlagOnCommands` (AC-J-2, FR-J.2): construct each current command's real `flag.FlagSet`, `VisitAll`, and reject the normalized forbidden spellings; a command-catalog guard compares tested command names against the root dispatch/help catalog so a new command cannot bypass inspection. RED until every current command flag is inspected. <!-- sdd-owner: implementation -->
- [x] J.5 RED — Write `internal/server/override_negative_test.go` with `TestOverrideInputDeniedFailClosed` (AC-J-3, FR-J.3): send JSON/MCP arguments containing each forbidden spelling through the real strict decoders for representative approval, judgment, reconciliation, review, and evidence-lifecycle mutations; expect the existing unknown-field/invalid-input typed denial and assert no state/event/receipt/idempotency change. Include at least one administrative/privileged role input proving no implicit bypass (NFR-J.1). RED if any surface silently accepts an override spelling or mutates state. <!-- sdd-owner: implementation -->
- [x] J.6 RED — In `cmd/drenyra-engram/no_override_surface_test.go`, add `TestCLIOverrideInputDeniedFailClosed` (AC-J-3): invoke representative privileged commands with `--override`, `--break-glass`, `--force`, `--bypass`; expect existing unknown-flag usage failure and no database change. RED if any flag is accepted or any state changes. <!-- sdd-owner: implementation -->
- [x] J.7 RED — Write `internal/store/authorization_no_bypass_test.go` with `TestDeniedOperationNotBypassableViaAdapterOrStore` (AC-J-3, FR-J.3): table rows invoke the same denied scenario at policy, server/service, and store boundaries; include an administrative principal and a same-principal SoD case; each layer returns the same frozen auth reason code and store snapshots remain unchanged. SoD anchors stay green (`purge_sod_test.go:74`, `review_store_test.go:555`, `review_test.go:229`, `purge_http_test.go:391`). RED until all three layers agree with zero state change. <!-- sdd-owner: implementation -->
- [x] J.8 GREEN — Freeze the override-absence decision in `contracts/provenance.md`: insert `## Frozen override-absence decision` immediately after `## Non-authorization boundary` and before migration semantics (AC-J-4, FR-J.4, FD-1), stating no override/break-glass/force/bypass/privileged-emergency path exists, that override audit is negative conformance, that privileged/urgent callers remain subject to the same versioned policy/scope/assurance/role/SoD rules, and that professional memory approval remains outside business/payment authorization. No contract version or schema version changes. <!-- sdd-owner: implementation -->
- [x] J.9 TRIANGULATE — Assert non-authorization boundary (AC-J-5, FR-J.5, NFR-XC-4): review J matrix rows, negative tests, decision record, and any new wiring for observation/denial-only semantics — no new surface approves, posts, files, or reopens writes; no business/payment authorization capability claim appears. <!-- sdd-owner: implementation -->

**Verification gate PR J:** `go test ./internal/authz ./internal/server ./internal/store ./cmd/drenyra-engram` then `go test ./...` green; `gofmt -l .` and `go vet ./...` clean; `git diff -- contracts/provenance.md` limited to the frozen override-absence section; `npm test` green.

---

## PR L — Idempotency replay matrix and lost-response proof (AC-L-1…AC-L-8)

Objective: one consolidated operation × surface replay catalog in `internal/server`, real MCP and CLI replay cases, and interrupted-reservation/lost-response recovery proof for approval, judgment, reconciliation, review, hold, and reopen. Files: `internal/server/idempotency_replay_matrix_test.go` (new), existing MCP test files (`internal/server/mcp_purge_test.go`, `mcp_judgment_test.go`, `mcp_reconciliation_test.go`), existing CLI command test files (`cmd/drenyra-engram/judge_test.go`, `reconcile_test.go`, `review_test.go`, `purge_test.go`, + retention/hold/reopen command tests where `--request-id` is exposed), `internal/store/idempotency_interrupted_reservation_test.go` (new).

- [x] L.1 RED — Write `internal/server/idempotency_replay_matrix_test.go` with `TestIdempotencyReplayMatrix` (AC-L-1, AC-L-5, AC-L-6, FR-L.1/FR-L.2/FR-L.6): the catalog shape `replayCase{operation, surface, fixture, firstRequest, sameRequest, changedPayload, changedPrincipal, assertOutcome, assertConflict, effectSnapshot, externalProof}`; one named row per `operation/surface` (EC-2), enumerating approve memory; judgment propose/confirm/withdraw; reconciliation propose/decide/confirm/withdraw; review reject; evidence lifecycle place/lift hold, retention-policy put; period reopen; purge request/approve/reject/execute + currently exposed idempotent cancel/withdraw; and sync second import. Each executable row proves replay returns the original stored outcome (`idempotentReplay=true` where exposed) and an unchanged before/after logical snapshot (entity/event/receipt/idempotency-completion counts). HTTP/MCP rows execute real handlers; CLI/sync rows set `externalProof` to the dedicated tests. A `proof-links-resolve` subtest parses package test declarations with `go/parser` and fails on a missing named external proof (FR-L.7 for `TestSyncIdempotent`, `TestCLISyncRoundTrip`). Conflict rows assert the frozen typed conflict (`auth.Code(err) == IDEMPOTENCY_CONFLICT` or per-operation typed conflict) for changed payload AND changed principal, never silent success and never duplicate effect. RED until every enumerated row is present and named. <!-- sdd-owner: implementation -->
- [x] L.2 GREEN — Implement the HTTP/MCP in-package rows of `TestIdempotencyReplayMatrix` with server-local fixtures and request builders; request IDs/timestamps are constants per subtest; `effectSnapshot` is logical (operation-specific entity/projection fields + event/receipt/idempotency-completion counts), never raw SQLite bytes. Existing replay/conflict anchors stay green (`approval_test.go:505/:544`, `judgment_test.go:677/:719/:763`, `reconciliation_test.go:685/:736/:763`, `review_store_test.go:589`, `hold_test.go:172/:189`, `retention_policy_test.go:178`, `reopen_test.go:237`, `receipt_emission_test.go:429`, `http_review_test.go:268`, `purge_http_test.go:279–297/:533–547`). <!-- sdd-owner: implementation -->
- [x] L.3 RED — Add `TestMCPPurgeRequestIdempotentReplay`, `TestMCPJudgmentProposeIdempotentReplay`, `TestMCPReconciliationProposeIdempotentReplay` in the owning MCP test files (AC-L-2, FR-L.3): issue the exact same tool call twice, decode both results, and assert the second result carries the first stored IDs and replay marker. The existing fresh-only tests (`mcp_purge_test.go:46`, `mcp_judgment_test.go:93`, `mcp_reconciliation_test.go:74`) remain but no longer stand alone. RED until each replay asserts stored outcome + replay semantics. <!-- sdd-owner: implementation -->
- [x] L.4 RED — Add CLI replay tests `TestCLIJudgeReplay`, `TestCLIReconcileReplay`, `TestCLIReviewRejectReplay`, `TestCLIPurgeRequestReplay` (+ rows for every other idempotent purge subcommand exposed by the parser, and retention/hold/reopen command replay where `--request-id` is exposed) in the owning command test files (AC-L-3, FR-L.4): run the command twice against the same temp DB with the same `--request-id`, decode stdout JSON, then open/read the store to compare event/receipt counts. Fresh-only assertions (`main_test.go:1137`, `judge_test.go:99`, `reconcile_test.go:109`, `review_test.go:160`, `purge_test.go:217`) are NOT sufficient. RED until each replay returns the stored outcome with no duplicate effect. <!-- sdd-owner: implementation -->
- [x] L.5 RED — Write `internal/store/idempotency_interrupted_reservation_test.go` with a `interruptedReservationCase{operation, seed, insert, invoke, code, snapshot}` descriptor (AC-L-4, FR-L.5): for approval, judgment, reconciliation, review, and hold, `insert` computes the command hash with the same package-private hash/shape helper used by production and inserts a row with entity/event/result/completed columns `NULL` into the operation's real idempotency ledger (`idempotency_keys`, `judgment_idempotency_keys`, `reconciliation_idempotency_keys`, `review_idempotency_keys`, `evidence_hold_idempotency_keys` respectively); every replay asserts the typed conflict (never a scan/JSON-decode/persistence error) and equal logical state/event/receipt counts — mirroring `purge_store_test.go:58/:121`; existing purge-execution recovery anchors (`purge_execution_test.go:559/:713/:802`) stay green. RED until each family proves a typed deterministic outcome. <!-- sdd-owner: implementation -->
- [x] L.6 RED — Add the reopen lost-response proof in the same file (AC-L-4, FR-L.5): reopen has no nullable reservation ledger, so prove BOTH sides of its atomic commit — (a) a temporary test trigger aborts insertion of the target `reopened` event after the projection update is attempted; call `ReopenPeriod`, assert rollback left the period closed, no event/receipt/request row, and a clean retry succeeds exactly once; (b) discard the first successful result and re-invoke the same command, asserting the existing event is replayed with `IdempotentReplay=true` and no duplicate event/receipt. The test doc comment states the distinction (atomic projection+event vs. nullable reservation). If the schema prevents the trigger mechanism, record the limitation and leave AC-L-4 red — a production reservation seam is an exception requiring re-approval. <!-- sdd-owner: implementation -->
- [x] L.7 TRIANGULATE — Assert no-duplicate-effects and determinism (AC-L-5, NFR-L.1/NFR-L.2): matrix rows and recovery tests assert unchanged receipt/event counts on replay/recovery; the two no-duplicate-receipt anchors (`receipt_emission_test.go:429`, `store/__tests__/receipt-emission.test.ts:305`) stay green; any matrix row exposing a real behavioral inconsistency keeps the criterion RED and is reported, never widened/deleted. <!-- sdd-owner: implementation -->

**Verification gate PR L:** `go test ./internal/server ./internal/store ./cmd/drenyra-engram` then `go test ./...` green; `gofmt -l .` and `go vet ./...` clean; `npm test` green.

---

## PR Q — Generated cross-tenant and nonexistence-safe matrix (AC-Q-1…AC-Q-5)

Objective: a Go test-only operation catalog over the canonical `internal/server/api.go` surface with an exhaustiveness guard, per-operation cross-tenant cases asserting nonexistence-safe outcomes and zero foreign-identifier leakage, and denied-mutation side-effect freedom. Files: `internal/server/cross_tenant_matrix_test.go` (new). No checked-in JSON, no production registry, no production change (FD-4, NFR-Q.2).

- [ ] Q.1 RED — Write `internal/server/cross_tenant_matrix_test.go` with `TestCrossTenantMatrixExhaustiveness` (AC-Q-1, FR-Q.1/FR-Q.2): compute the exported method set with `reflect.TypeOf((*API)(nil))` (reflect allowed per D-3), compare it with unique `method` values in the catalog; the catalog must include every canonical method (Save, Get, GetByTopic, Chain, Search, Context, Relations, Transitions, Doctor, StoreObject, GetObject, PutRetentionPolicy, ResolveRetentionPolicy, EvaluatePurgeEligibility, PlaceHold, LiftHold, ActiveBlockingHolds, HoldsForObject, RequestPurge, ApprovePurge, RejectPurge, CancelPurge, WithdrawPurge, FinalizePurge, ExportEvidenceLifecycle, ReviewQueue, Reconstructibility, RuleShow, RuleHistory, RuleImpact, ReviewDetail, RejectMemory, ReturnMemory, Compare, Approve, Reject, Void, Supersede, LinkEvidence, LinkRules, LinkRuleVersion, Judge, PeriodSummary, FindPeriodClosure). Any method missing a case makes the test RED. RED until every exported API method is classified (`read`/`mutation`/`observation`). <!-- sdd-owner: implementation -->
- [ ] Q.2 GREEN — Implement the catalog data model (`operationKind`, `safeOutcome`, `apiOperationCase{method, kind, seedTenantA, invokeAsB, safeOutcome, assertSafe, snapshotA}`) and `crossTenantFixture` (one fresh in-memory store, two exact company scopes with different organization/company/RUC/period identifiers, per-row fresh fixture). Implement `TestCrossTenantMatrixExhaustiveness` green once every method is classified. Deterministic, exact structural scope, seeded in-memory per case, no credentials/customer data (NFR-Q.1). <!-- sdd-owner: implementation -->
- [ ] Q.3 RED — Implement `TestCrossTenantMatrix` scenarios for every catalog operation (AC-Q-2, AC-Q-4, FR-Q.3/FR-Q.5): `seedTenantA` creates minimum valid tenant-A state via existing store/server fixture helpers; `invokeAsB` always supplies tenant B's exact scope and a verified tenant-B principal, intentionally passing tenant-A resource IDs where the signature permits; `assertSafe` pins the operation's frozen contract (object → `OBJECT_NOT_FOUND` `object_http_test.go:92`; retention resolve → zero policy `mcp_retention_policy_test.go:284`; reconstructibility → zero denominator `mcp_reconstructibility_test.go:121`/`reconstructibility_http_test.go:253`; chain/search/context/review queues/history → empty; policy-frozen scope denial where already frozen `period_service_test.go:214`) and rejects any tenant-A organization/company/RUC/period/resource identifier in the serialized result/error. RED for any row that leaks foreign existence/data, and RED for any ID-only/global method that cannot express tenant-B scope (test diagnostic such as `canonical API.Get cannot express caller scope`) — never mark N/A, never call the store directly, never claim adapter scoping proves the canonical method. Isolation anchors stay green (`search_test.go:77`, `object_hardening_test.go:342`, `mcp_object_test.go:91`, `mcp_retention_policy_test.go:284`, leakage probes EXACTLY 0). <!-- sdd-owner: implementation -->
- [ ] Q.4 RED — Implement `TestCrossTenantMutationNoSideEffect` (AC-Q-3, FR-Q.4): filter `kind == mutation`, capture `snapshotA` before and after each denied cross-tenant mutation, require equality — no state change, no event, no receipt, no idempotency completion, no external side effect; snapshot is a deterministic logical digest over tenant-A rows relevant to the operation plus event/receipt/idempotency-key/completion/relation/transition/object-byte hashes (never a live WAL file). RED until every denied mutation leaves all counters and the digest unchanged. <!-- sdd-owner: implementation -->
- [ ] Q.5 TRIANGULATE — Determinism and non-weakering (NFR-Q.1/NFR-Q.2): assert deterministic per-case outcomes, exact structural scope (never post-filter), and that any isolation defect exposed keeps the criterion RED and is reported for a separately justified correction — never a weakened safe-outcome assertion. Confirm no production change and no JSON/second registry introduced (FD-4). <!-- sdd-owner: implementation -->

**Verification gate PR Q:** `go test ./internal/server ./internal/store ./internal/search` then `go test ./...` green; `gofmt -l .` and `go vet ./...` clean; `npm test` green.

---

## PR G — Direct upgrades, interruption recovery, and migration provenance (AC-G-1…AC-G-5)

Objective: a genuine v1…v13 → v14 direct-upgrade matrix through the normal `Open` path, deterministic migration interruption/reopen proof, and the frozen doc-only migration provenance decision. Files: `internal/store/migration_direct_upgrade_test.go` (new), `contracts/provenance.md`. No production migration-history table or schema v15 (FD-2, FR-G.3).

- [ ] G.1 RED — Write `internal/store/migration_direct_upgrade_test.go` with `TestDirectUpgradeMatrixV1ToV14` (AC-G-1, FR-G.1): `legacyFixture{version, build, seed, assertPreserved}`; builders reuse frozen historical helpers (v1 `v1SchemaDDL`; v2/3/4/5/7/8/9/10/11/13 via `openV2Schema`…`openV13Schema`; v6 via `openV5Schema` + real `migrateV5ToV6`; v12 via `openV11Schema` + real `migrateV11ToV12`); each builder asserts `readSchemaVersion == source version` before seeding and closes the raw handle before normal `Open`. RED until all v1…v13 rows exist. <!-- sdd-owner: implementation -->
- [ ] G.2 RED — Seed representative additive rows per source version (AC-G-1, NFR-G.1): at the first version where a feature exists, seed approval/membership, judgment, reconciliation, materiality/close, structured policy data, receipt/key, evidence object, retention policy, hold, purge lifecycle, review decision, and legacy unversioned rule link rows; store exact scalar/JSON/blob bytes + row counts; carry forward all applicable representatives across later versions. RED until every v1…v13 row has its seed. <!-- sdd-owner: implementation -->
- [ ] G.3 GREEN — Implement the upgrade assertions for each row (AC-G-1, FR-G.1): call normal `Open(path)` exactly once; assert `schema_version == 14` (`store.go:185`); compare every seeded value + relevant row count with the pre-open snapshot; assert the final invariant manifest (all v14 tables/triggers/indexes from existing per-version helper lists, `rule_links.version`, `rule_links.effective_at`, `idx_rule_links_ref`; no staging tables such as `receipts_v*` remain); run representative immutable/no-delete/fail-closed checks exposed by migrated structures. Existing per-step tests stay green (`store_test.go:722`, `migration_v3_test.go:151/:211`, `migration_v4_test.go:188/:251`, `migration_v5_test.go:126/:185/:231`, `migration_v6_test.go:167/:331`, `migration_v7_v8_test.go:177`, `migration_v9_test.go:233`, `migration_v10_test.go:145`, `migration_v11_test.go:399`, `migration_v12_test.go:269`, `migration_v14_test.go:151/:196/:227`). GREEN only after every row proves schema + preservation + invariants, not version alone. <!-- sdd-owner: implementation -->
- [ ] G.4 RED — Write `TestMigrationInterruptedRollsBackToPriorVersion` (AC-G-2, FR-G.2): build a genuine seeded v13 database; before closing the raw handle create `idx_rule_links_ref` with a deliberately wrong definition; normal `Open` enters `migrateV13ToV14`, applies earlier transactional ALTER work, then fails at index creation (per D-5); using a new raw read handle assert `schema_version` is still 13, `rule_links.version`/`rule_links.effective_at` are absent (proving earlier statements rolled back), seeded observation + legacy rule-link bytes unchanged, and no staging/partial artifacts or duplicate rows. Document precisely that this proves SQLite transaction rollback under the one-transaction-per-step model (`store.go:562–570`/`:900+`), not an OS kill (NFR-G.2). RED until rollback coherence is proven. <!-- sdd-owner: implementation -->
- [ ] G.5 GREEN — Write `TestMigrationCrashReopenConvergesToV14` (AC-G-2, FR-G.2): drop only the deliberately wrong index, close the raw handle, call normal `Open` again; assert convergence to v14, exact data preservation, the correct index definition, and one copy of each seeded row. Unknown/malformed versions fail closed (`migration_v5_test.go:231`). GREEN only after safe reopen/re-run with no partial artifacts or duplicate effects. If the seam is non-injectable, AC-G-2 stays RED and the residual gap is documented — a production migration hook or ledger is not authorized. <!-- sdd-owner: implementation -->
- [ ] G.6 GREEN — Freeze the migration provenance decision in `contracts/provenance.md` (AC-G-3, AC-G-4, FR-G.3/FR-G.4, FD-2): replace the stale literal `schema_version = 2` wording with version-neutral policy text + a new `### Migration provenance for schema v14 (frozen)` subsection stating ordered migration functions and reviewed code order; one transaction per step with schema version advanced last; final `schema_version=14` as the authoritative release provenance record; no migration-history table, per-deployment event ledger, or schema v15; proves current schema generation and deterministic code path; does NOT prove wall-clock execution history, operator identity, or per-deployment migration events. Reviewer check confirms no production table/schema v15 introduced. <!-- sdd-owner: implementation -->
- [ ] G.7 TRIANGULATE — Fixture fidelity and non-rollback (NFR-G.1/NFR-G.2): confirm builders derive from frozen migration-era schemas/tests and preserve representative rows; confirm failure-injection does not alter the one-transaction-per-step model; confirm assertions verify structural invariants and data preservation, never version numbers alone. <!-- sdd-owner: implementation -->

**Verification gate PR G:** `go test ./internal/store` then `go test ./...` green; `gofmt -l .` and `go vet ./...` clean; `git diff -- contracts/provenance.md` limited to the migration-provenance subsection; `npm test` green.

---

## PR Z — Operability evidence and reconciliation (AC-Z-1…AC-Z-4)

Objective: one operability evidence document with executable citations, a qualitative recovery-objective statement (no invented numbers), and reconciliation of stale DEFERRED drill claims across the audit register, v1 gate, and evidence-object doc, plus a structural readback test. Files: `docs/architecture/operability-evidence.md` (new), `docs/architecture/evidence-object-v0.7.md`, `docs/product/initial-market-and-v1-gate.md`, `internal/store/operability_documentation_test.go` (new). No RTO/RPO/SLA/SLO number anywhere (FD-3).

- [ ] Z.1 RED — Write `internal/store/operability_documentation_test.go` as a structural readback test (AC-Z-1…AC-Z-4, FR-Z.1/FR-Z.2/FR-Z.3): locate the repo root from the test file path, read the four documents, check required headings/citations, check the stale drill-deferral phrases are removed from the named blocks, and reject numeric RTO/RPO/SLA/SLO target patterns in changed artifacts. RED until the docs satisfy the readback contract. <!-- sdd-owner: implementation -->
- [ ] Z.2 GREEN — Create `docs/architecture/operability-evidence.md` (AC-Z-1, FR-Z.1, D-6) with exactly: (1) Purpose and evidence boundary; (2) Delivered capabilities and executable evidence table — routine/full doctor (`internal/store/doctor.go`, `doctor_test.go:67/:122/:171`, `object_hardening_test.go:242/:300/:319/:634`, `api_test.go:363/:425`, `main_test.go:261`), WAL-safe snapshot (`drill.go::CreateDrillSnapshot` :134), restore + verify-after-restore (`TestRunRestoreDrillSuccess` :404, negative matrix :487, scope isolation :678, schema-version fail-closed :762), corruption drills on marked copies only (`drill_test.go:162/:256/:287/:322/:353`), exact scope + backup-identity verification, tenant export (`evidence_export_test.go:129/:362/:507`, `purge_http_test.go:637`); (3) Qualitative recovery objectives stating snapshot/restore/post-restore verification/corruption detection are proven mechanisms and that numerical RTO/RPO targets are deployment/business-owned and UNKNOWN until an accountable owner records them; (4) Operational boundaries and non-claims (no deployment backup schedule, retained recovery points, external drill, guaranteed recovery time/data loss, encryption-at-rest/TDE, cloud storage, production service readiness); (5) Evidence maintenance (removal/failure of cited proof restores conservative RISK wording). No number/duration/percentile/target anywhere (AC-Z-2). <!-- sdd-owner: implementation -->
- [ ] Z.3 GREEN — Reconcile `docs/architecture/evidence-object-v0.7.md` lines 58–59 (AC-Z-3, FR-Z.3): replace the single "Production object-store operations" deferred bullet with a split statement — repository-local snapshot/restore and marked-copy corruption drills delivered with `drill.go`/test citations; cloud/remote operations, encryption-at-rest/TDE, deployment-owned numerical recovery objectives, and externally run production drills explicitly unproven. Every reconciled statement pairs with its executable citation (NFR-Z.1). <!-- sdd-owner: implementation -->
- [ ] Z.4 GREEN — Reconcile `docs/product/initial-market-and-v1-gate.md` G-5 and G-6 (AC-Z-3, FR-Z.3): G-5 removes only "production backup/restore drills" from the stale deferred list and cites `operability-evidence.md`, retaining cloud/remote storage, scheduler executor, OCR/content search, and unrelated unmet items; G-6 records restore/corruption drills as repository-delivered with test citations but retains a bounded PARTIAL classification unless every other G-6 element is proven by its own current evidence (race/fuzz/cross-tenant prerequisites not all closed). No whole-gate MET claim on this slice. <!-- sdd-owner: implementation -->
- [ ] Z.5 TRIANGULATE — Bounded PASS and no invented numbers (AC-Z-2, AC-Z-4, FR-Z.4, FD-3): assert the recovery-objective wording is qualitative, that no numeric recovery target appears in any changed artifact of this change, and that the PASS scope is bounded to repository-verifiable capabilities (not a production SLO, not a completed external drill, not guaranteed recovery time/data loss). <!-- sdd-owner: implementation -->

**Verification gate PR Z:** `go test ./internal/store` then `go test ./...` green (doc readback test green); `gofmt -l .` and `go vet ./...` clean; `npm test` green; grep over changed docs confirms zero numeric RTO/RPO/SLA/SLO targets.

---

## PR evidence-pass — Audit register closure and whole-change gates (AC-L-8, AC-J-5, AC-Q-5, AC-G-5, AC-Z-4, AC-XC-1…AC-XC-6)

Objective: flip each of the five audit rows to bounded PASS only after its full evidence has landed with per-claim citations, and run the whole-change gates. Files: `docs/due-diligence/2026-08-product-architecture-audit.md`, and verification over the entire changed diff. No block flips on prose alone (EC-4, NFR-XC-5).

- [ ] E.1 GREEN — Update the audit register rows G/J/L/Q/Z in `docs/due-diligence/2026-08-product-architecture-audit.md` to bounded PASS (AC-L-8, AC-J-5, AC-Q-5, AC-G-5, AC-Z-4, EC-1/EC-3): L (186–194) cites the matrix + lost-response tests and removes the complete-surface RISK qualifier; J (159–175) cites the consolidated matrix, negative tests, and decision, with PASS wording that does NOT claim production identity readiness and asserts the non-authorization boundary (block I adjacency retained unchanged); Q (277–296) cites the generated matrix and guard and removes the exhaustive-coverage RISK qualifier; G (111–122) cites the direct-upgrade matrix, interruption/reopen proof, and provenance decision and removes the operations RISK qualifier; Z (420–433) is `PASS for repository-demonstrated operability / RISK for deployment objectives` with citations to `operability-evidence.md` and executable tests. Each PASS claim pairs with a unique test name or frozen contract path+section (EC-1). Apply each row ONLY after its block's suite is green; a reverted/insufficient slice restores the prior RISK status rather than leaving an unsupported PASS (NFR-XC-5). <!-- sdd-owner: implementation -->
- [ ] E.2 VERIFY — Whole-change gates (AC-XC-1…AC-XC-6): final `go test ./...`, `npm test`, `npm run typecheck`, `go vet ./...`, and `gofmt -l .` are green in config `verify_order`; `go test ./internal/core -run TestGoldenVectorsGo` green (no shared Go↔TS semantic change, AC-XC-2); reviewer greps confirm no float money (AC-XC-3, NFR-XC-3), no new authority surface (AC-XC-4, IR-3), no new production schema/override path/public API operation/numerical recovery promise (AC-XC-6), and no `any` in TypeScript (IR-5). Any discovered shared-contract correction must preserve golden parity and be separately approved — never an incidental fix (NFR-XC-2). <!-- sdd-owner: implementation -->
- [ ] E.3 TRIANGULATE — Register-only deltas (NFR-XC-5): confirm the audit diff contains ONLY the five G/J/L/Q/Z row updates plus per-claim citations and the Z deployment-objective caveat; adjacent UNKNOWN/RISK classifications (block I identity readiness, production-readiness caveats) are byte-identical. <!-- sdd-owner: implementation -->

**Verification gate PR evidence-pass:** config `verify_order` green (`npm run typecheck` → `go vet ./...` → `gofmt -l .` → `go test ./...` → `npm test`) + `go test ./internal/core -run TestGoldenVectorsGo`; audit diff limited to the five intended rows.

---

## Verification gates per PR

- PR J: `go test ./internal/authz ./internal/server ./internal/store ./cmd/drenyra-engram` then `go test ./...`; provenance diff limited to override-absence section.
- PR L: `go test ./internal/server ./internal/store ./cmd/drenyra-engram` then `go test ./...`.
- PR Q: `go test ./internal/server ./internal/store ./internal/search` then `go test ./...`.
- PR G: `go test ./internal/store` then `go test ./...`; provenance diff limited to migration-provenance subsection.
- PR Z: `go test ./internal/store` then `go test ./...` (doc readback green); no numeric recovery target in changed docs.
- PR evidence-pass: config `verify_order` + golden parity; audit diff limited to the five rows.
- Full chain gate per PR boundary: `npm run typecheck` → `go vet ./...` → `gofmt -l .` → `go test ./...` → `npm test`.

## Cross-cutting checklist

- [ ] Conventional commit per atomic milestone (`test:`, `docs:`, `refactor:`); no AI attribution. <!-- sdd-owner: implementation -->
- [ ] Strict TDD per slice: the named failing tests land RED before their fixtures/helpers/docs; green at each slice boundary with `go test ./...` and `npm test`. <!-- sdd-owner: implementation -->
- [ ] Money stays whole int64 cents / BigInt cents; no float path in any helper, matrix, fixture, or doc (IR-1, NFR-XC-3). <!-- sdd-owner: implementation -->
- [ ] Scope stays structural and fails closed on mismatch; cross-tenant invisibility is a tested invariant (IR-2). <!-- sdd-owner: implementation -->
- [ ] Non-authorization boundary intact: no new surface approves, posts, files, or reopens writes (IR-3, NFR-XC-4); override absence is negative conformance only (FD-1). <!-- sdd-owner: implementation -->
- [ ] No shared Go↔TS semantic change; golden parity preserved if any shared contract is touched (IR-4, NFR-XC-2). <!-- sdd-owner: implementation -->
- [ ] No `any` in TypeScript (IR-5); fixtures/docs contain no credentials, tokens, or customer data (NFR-XC-7). <!-- sdd-owner: implementation -->
- [ ] No new production schema, override path, public API operation, migration-history table, schema v15, or numerical recovery promise (NFR-XC-6, FD-2, FD-3, FD-4). <!-- sdd-owner: implementation -->

## Definition of done

- [ ] All tasks checked; every acceptance criterion AC-L-1…AC-L-8, AC-J-1…AC-J-5, AC-Q-1…AC-Q-5, AC-Z-1…AC-Z-4, AC-G-1…AC-G-5, and AC-XC-1…AC-XC-6 in `spec.md` verified green by its mapped test or frozen contract per the traceability table in `design.md`. <!-- sdd-owner: implementation -->
- [ ] Full gates per config verify_order (`npm run typecheck` → `go vet ./...` → `gofmt -l .` → `go test ./...` → `npm test`) green at every PR boundary, plus `go test ./internal/core -run TestGoldenVectorsGo`. <!-- sdd-owner: implementation -->
- [ ] The five audit rows flip to bounded PASS only after each block's full evidence lands with per-claim citations; no prose-only PASS exists (NFR-XC-5, EC-1–EC-4). <!-- sdd-owner: implementation -->
- [ ] Chain fully merged to main via stacked-to-main; delivery and chain strategy recorded before apply (Review Workload Guard decision). <!-- sdd-owner: parent -->

## Parent lifecycle gates (post-apply, grouped)

- [ ] Start or reuse bounded review for each stacked-to-main PR boundary after its normalization + candidate freeze (PR J → L → Q → G → Z → evidence-pass); one correction budget per candidate; no reviewer launched by apply. <!-- sdd-owner: parent -->
- [ ] After the final evidence-pass PR merges, run the verify phase (`sdd-verify`) against AC-L/J/Q/Z/G/XC criteria and this tasks list; remediate only through the bounded correction path. <!-- sdd-owner: parent -->
- [ ] Archive the change only when verify reports all criteria green and the full chain is merged. <!-- sdd-owner: parent -->
