# Goreleaser configuration — drenyra-engram releases.
#
# Audit-trail surface (private product policy): every release ships
#   - cross-platform static binaries
#   - SHA-256 checksums (artifact checksum verification)
#   - SPDX SBOM (software bill of materials for supply-chain review)
#   - signed provenance via the release workflow (SLSA/attestations)
# See RELEASING.md for the full release process.

version: 2

project_name: drenyra-engram

before:
  hooks:
    - go mod tidy

builds:
  - id: drenyra-engram
    main: ./cmd/drenyra-engram
    binary: drenyra-engram
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    flags:
      - -trimpath
    ldflags:
      - -s -w -X main.version={{.Version}}

archives:
  - formats: [tar.gz]
    format_overrides:
      - goos: windows
        formats: [zip]
    files:
      - LICENSE
      - README.md

checksum:
  name_template: "checksums.txt"

sboms:
  - id: spdx
    artifacts: binary
    documents:
      - "{{ .Binary }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}.spdx.json"
    cmd: syft
    args:
      - "$artifact"
      - "--output"
      - "spdx-json=$document"

release:
  draft: false
  prerelease: auto
  footer: |
    ---
    Published from the private release workflow. Artifacts verified via
    checksums.txt; SBOMs attached per binary for supply-chain review.
